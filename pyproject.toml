[project]
name = "crypto-alpha-data-layer-validator"
version = "0.1.0"
description = "Local validator for crypto-alpha P1 data layer (JSON Schema contracts and fixtures)"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.0,<5.0",
]

[tool.uv]
managed = true
