<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan.

## Current spec under development

The current spec branch is `002-data-layer` (P1 in the spec-001/plan.md
phasing). Read `specs/002-data-layer/plan.md` first. The local validator
at `scripts/validation/validate_data_layer.py` is the test surface for
P1; run it with `python scripts/validation/validate_data_layer.py`
after `uv venv && source .venv/bin/activate && uv pip install -e .`.
<!-- SPECKIT END -->
